#include <C:\Users\randy\source\repos\Project3\Project3\Hack.h>
// Project on matching coders w/ coders
int main() {
	FILE* infile = 0; // Prints input to file

	outfile = fopen("outfile.txt", "w");

	FILE* outfile = NULL; // Takes input and sorts it ------------------------------------ignore for now

	//getInfo(outfile);
	 getHelp("%d",outfile);
	 getLang("%d", outfile);
	 getExp("%d", outfile);

	fclose('outfile');

	return 0;
};